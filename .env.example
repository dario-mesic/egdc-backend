# ===================================================================
# Database credentials (used by the 'db' service in docker-compose)
# ===================================================================
POSTGRES_USER=myuser
POSTGRES_PASSWORD=mypassword
POSTGRES_DB=egdc_db

# ===================================================================
# Backend database connection
# Host MUST be 'db' (the Docker service name) when running in Docker.
# The credentials here must match POSTGRES_USER / POSTGRES_PASSWORD / POSTGRES_DB above.
# ===================================================================
DATABASE_URL=postgresql+asyncpg://myuser:mypassword@db:5432/egdc_db

# ===================================================================
# Backend application settings
# ===================================================================
SECRET_KEY=change-me-to-a-long-random-string
BACKEND_CORS_ORIGINS=["http://localhost:3000","http://localhost:8000"]

# ===================================================================
# Frontend (passed as a build arg to Next.js)
# ===================================================================
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
